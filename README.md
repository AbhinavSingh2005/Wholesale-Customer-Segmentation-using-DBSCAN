# Wholesale-Customer-Segmentation-using-DBSCAN
This project applies the DBSCAN clustering algorithm to the Wholesale Customer Segmentation dataset. Data is standardized, clustered based on density, and visualized using PCA. The model identifies customer groups and outliers without requiring a predefined number of clusters.
